name: versions
fill:
  official-link: ${$GetLink|Official}
  version-image-link: ${$GetLink|ImageSta}
  version-id: ${file_name}
  title: ${file_name}
  templates:
    - VersionPlaceholder
    - VersionMarkdownCard
    - Raw