<Border Style="{StaticResource VersionImageBorder}">
<local:MyImage Source="${version-image-link}" ${@IF|${version-image-link-fallback}|FallbackSource="${version-image-link-fallback}" } Style="{StaticResource VersionImage}"/>
</Border>
<Border Style="{StaticResource VersionTitleBorder}">
<TextBlock Style="{StaticResource VersionTitleBlock}" Text="${version-id}" />
</Border>