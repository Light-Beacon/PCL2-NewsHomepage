components:
 - $MarkdownPresenter
base: LucaFrame
containers: base -> FlowDocumentViewer -> this
fill:
  title: ${@GetMdH1}
  line-color: {DynamicResource ColorBrush3}
filter:
  header-image-link: $HASVALUE