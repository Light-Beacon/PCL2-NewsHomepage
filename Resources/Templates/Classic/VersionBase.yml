components:
 - ${@IF|${warn}|Head/Warn}
 - ${@IF|${hint}|Head/Hint}
 - ${@IF|${version-image-link}|Classic/VersionHeaderImage}
 - FlowDocumentViewer
 - ${@IF|${not_finished}|Loading}
 - ${@IF|!${disable-websites}|Classic/VersionWebsites}
 - ${@IF|!${disable-toolbar}|Classic/VersionToolBar}
base: Card
fill:
   loading_content: 翻译施工中